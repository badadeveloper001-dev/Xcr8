# Integrations

This module hosts platform adapters for social media distribution. Each adapter should expose:

- content normalization
- API payload mapping
- publish scheduling hooks
- analytics retrieval hooks
