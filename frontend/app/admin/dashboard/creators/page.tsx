"use client";
import { useMemo, useState } from "react"; import { useQuery } from "@tanstack/react-query"; import { getAdminCreators } from "@/lib/api";
export default function CreatorsPage(){const [q,setQ]=useState("");const accessCode=useMemo(()=>typeof window==="undefined"?"":sessionStorage.getItem("xcr8-admin-access")??"",[]);const {data,isLoading}=useQuery({queryKey:["admin-creators",q],queryFn:()=>getAdminCreators(accessCode,q),enabled:Boolean(accessCode)});const items=data?.items??[];return <section><h2 className="xcr8-title-lg text-white light:text-slate-900">Creators</h2><p className="mb-4 text-sm text-slate-400">Search and support every creator from one place.</p><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search name or email" className="xcr8-input mb-4 max-w-md"/><div className="xcr8-panel divide-y divide-white/10 rounded-2xl">{items.map(c=><article key={c.user_id} className="flex flex-wrap items-center justify-between gap-3 p-4"><div><p className="font-medium text-white light:text-slate-900">{c.display_name}</p><p className="text-xs text-slate-500">{c.email}</p></div><p className="text-sm text-slate-400">{c.posts} posts · {c.platforms.join(", ")||"No platforms"}</p></article>)}{!isLoading&&!items.length?<p className="p-4 text-sm text-slate-400">No creators found.</p>:null}</div></section>;}